Step 2.3: Unreachable Transition Detection — Implementation Complete

Summary:
- Added unreachable transition detection in src/compiler/validation.rs
- After duplicate ID validation, iterates each machine's states and transitions
- Tracks whether an always-true transition (when: None) has been seen
- Flags all transitions following an always-true transition as unreachable
- Error format: "unreachable transition in machine '{id}', state '{state_name}', transition {index}"
- CompileErrorKind::UnreachableTransition is used (already defined in error.rs)

Validation:
- cargo test: 37 tests passed, 0 failed, no regressions
- All warnings are pre-existing unused imports (compiler module not yet wired into main)

Files changed:
- src/compiler/validation.rs (+21 lines)

Commit:
- 5724ac5: "Step 2.3: Implement unreachable transition detection"
